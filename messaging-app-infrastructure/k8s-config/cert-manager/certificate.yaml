apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: acme-crt
spec:
  secretName: acme-crt-secret
  dnsNames:
  - <placeholder>
  issuerRef:
    name: letsencrypt-issuer
    kind: ClusterIssuer